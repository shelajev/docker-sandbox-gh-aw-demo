# Registration requirements

- Email addresses are treated case-insensitively.
- Registration must reject an address if any case-equivalent address already exists.
- The production database is PostgreSQL.
- Integration tests must use PostgreSQL rather than H2 or a mock database.

